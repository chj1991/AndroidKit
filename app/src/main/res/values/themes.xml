<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Base.Theme.AndroidKit" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.AndroidKit" parent="Base.Theme.AndroidKit" />

    <!-- APP-05：Android 12+ SplashScreen 兼容主题 -->
    <style name="Theme.AndroidKit.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.AndroidKit</item>
    </style>
</resources>
